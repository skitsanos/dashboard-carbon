export const paginationProps = {
    pageSize: 10,
    pageSizes: [
        10,
        20,
        30,
        40,
        50
    ]
};

export const columnProps = {
    sm: 16,
    md: 8,
    lg: 3
};